import React, { Component } from 'react';

import './todo.scss';

export default class TodoView extends Component {
  render(){
    return (
      <section>
        <h1>Todos:</h1>
        <ul>Todo</ul>
      </section>
    );
  }
}
